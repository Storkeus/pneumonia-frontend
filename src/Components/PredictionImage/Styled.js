import styled from "styled-components";

export const StyledPredictionImageContainer = styled.div`
  display: inline-block;
  position: relative;
  margin: 0 auto;
`;

export const StyledPredictionImage = styled.img`
  width: 100%;
  max-width: 60rem;
  height: auto;
  display: block;
`;

export const StyledPredictionImageBox = styled.div`
  border: 0.3rem solid red;
  position: absolute;
`;
