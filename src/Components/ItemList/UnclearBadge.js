import React from "react";
import { StyledItemListUnclearBadge } from "./Styled";

/**
 * Render badge representing healthy lungs
 */
const UnclearBadge = () => {
    return <StyledItemListUnclearBadge>Niejednoznaczne</StyledItemListUnclearBadge>;
};

export default UnclearBadge;
